{
  "name": "cypress",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "android_headed": "npx cypress run --browser chrome --headed --config-file cypress2.config.js",
    "web_headed": "npx cypress run --browser firefox --headed --config-file cypress.config.js",
    "android_headless": "npx cypress run --browser chrome --config-file cypress2.config.js",
    "web_headless": "npx cypress run --browser firefox --config-file cypress.config.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^12.17.1"
  }
}
